const config = require('../config')

const sqlConnection = config.knex;

const userController = {

    login: (req, res) => {
    },

    register: (req, res) => {

    },

    searchProducts: (req, res) => {

    }
}

module.exports = userController